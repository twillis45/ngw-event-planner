# HANDOFF — 2026-09-09 (session continuation doc)

Written for a fresh session with no memory of this conversation. `HANDOFF.md`
(one directory up) is the primary, always-current state doc — read that first.
This file exists because `machine-handoff` asks for reasoning captured from the
conversation itself, not re-derived from the diff.

## In flight

Nothing is mid-edit. Every change discussed in this conversation is committed
and pushed; `git status` is clean on `main` at `origin/main`. The last real
push was `0d1493f6` / `25391773` / `c81161d2` (three small follow-on commits),
all CI-green, confirmed by direct `gh run list` checks, not by trusting a
push's own exit code.

## Decisions made this session (the part the code doesn't show)

- **All six "Passed +cond." stage cards on the artifact were re-audited by an
  independently-dispatched subagent**, not by re-reading my own prior writing —
  the session's standing rule is that a self-audit shares the blind spot with
  the work it's checking. Two real staleness bugs were found and fixed: the
  masthead HEAD SHA and a stage-8 line that said "CI green on HEAD, 0
  unpushed" at a moment when it was false (CI was 4 commits behind local HEAD).
  That line is now phrased as a point-in-time claim rather than a live one —
  **a commit that names "current HEAD" can never cite the SHA it produces.**
  If you see that pattern again on this page, that is why.
- **Stage 5's six open items were given the same "why unchecked" treatment**
  stage 9's items got earlier — sourced from the page's own "Waiting on" panel,
  nothing invented.
- **HANDOFF.md itself had two stale claims corrected**, found only because the
  user asked me to find real, non-owner-only work rather than just report
  gate status: it said the solemn Repast state "STILL OPEN, and genuine" and
  that `boardMatrix`'s loop-advance probe still skipped 4 of 6 states. Both had
  actually been fixed and verified by commit `26d3bd71` a day before HANDOFF's
  last stamp — the file just never caught up. I re-ran the test myself (Node
  20, `desktop` project) before writing the correction; I did not trust the
  commit message.
- **The push discipline held throughout**: never push over an in-flight CI
  run. Every push in this session was gated on `gh run list --branch main
  --json status` showing zero in-flight runs first, using background watchers
  when needed.

## Next steps, in order

1. **Nothing is queued from the assistant side.** The remaining open items on
   the artifact (six stage-5 security opens, PostHog/Sentry console arrival
   proof, the stranger-proof test, the rollback rehearsal, phase-3 economics)
   are all owner-only acts — reading a Supabase dashboard, running a real
   person through onboarding, rehearsing a rollback against the live deploy.
   None of them are something a session can produce.
2. If Todd asks for a stage-9 board sitting: it will not pass. Two prior
   board panels already refused it for the same reason (three retroactive
   gates have never been run, not run-and-failed). Convening again without
   new inputs just reproduces that finding.

## Watch items

- **The artifact republish notification fired multiple times this session**
  for `https://claude.ai/code/artifact/7f14f1d1-209a-4686-8615-61564542f6db`
  ("The First Recorded Gate"). It was never re-read after the last of those —
  if the next session edits it, **re-read it live first** (`Artifact` tool,
  `action: "read"`), don't trust the locally-cached copy.
- **`gate:cra` fails locally and passes in CI on the same commit.** Root
  cause undetermined; CI is authoritative, and the divergence is recorded in
  the artifact's Corrections section rather than papered over. Don't re-open
  this as a new finding — it's known and named.
- **`template-products` is no longer inside this repo.** It moved to its own
  sibling repo, `~/Code/ngw-template-products`, sometime during this session
  (commit `7741209`, "Drop the template-products ignore; it is a sibling
  now"). If a skill or doc still refers to `demo/template-products/` or
  similar, that path is stale.

## How to verify

```bash
cd ~/Code/ngw-event-planner/demo
git log --oneline -5                 # expect c81161d2 at or near HEAD
git status --short                   # expect clean
node scripts/handoff-freshness.mjs   # expect "within 3 commits"
gh run list --branch main --limit 3 --json status,conclusion,headSha \
  --jq '.[]|"\(.status)/\(.conclusion // "-") \(.headSha[0:8])"'
  # expect the two most recent runs both completed/success
```

To re-check the artifact instead of trusting this doc:
```
Artifact tool, action: "read", url: "https://claude.ai/code/artifact/7f14f1d1-209a-4686-8615-61564542f6db"
```
