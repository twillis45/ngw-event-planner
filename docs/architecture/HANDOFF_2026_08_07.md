# Handoff — 2026-08-07

**PR #82 IS MERGED AND DEPLOYED.** 128 commits on `main` (`208ebbb3`), Pages deploy
succeeded, and the shipped hostv2 was PROVEN fresh — not assumed.

```
live      https://twillis45.github.io/ngw-event-planner/hostv2/
chunk     HostShellV2-85e3a644.js   200 · 870,329 bytes
proof     5 code markers present in the live bundle: "Find someone", "rtool-lens",
          "Can't make it", "open to change it", "One name per line"
gates     jest 381/5647 · matrix 374 passed / 0 failed · all 5 CI checks green
tree      clean
```

**THE STALENESS TRAP IS REAL AND I HIT IT.** `index.html` never names `HostShellV2` —
it is lazy-loaded from inside `index-<hash>.js`. Checking index.html proves nothing.
Read the hash out of the index chunk, then grep the chunk for code you know is new.

---

## START HERE — the one thing worth doing first

**Drive the Santa Fe trip end to end**, against the live build.
Entry: Lodging Cockpit → **"Load the Santa Fe example"**. It is not a seeded event id.

Its lodging suite is 54/54 across six viewports, but that only covers the cockpit. The
JOURNEY — creation → ignition → hero ask → programme → food → budget → risks → day-of —
has no coverage, and that is where the unknowns are. A greppable check cannot answer
"can a host carry a multi-day trip to completion"; only a drive can.

---

## OPEN, in the order I would take them

### 1. Google hotel URL — a single-property paste slips the guard  (SMALL, precise)
A pasted `/travel/hotels/entity/…` URL is classified as a RESULTS page and falls into
`extractHotelCandidates`. The board fixed this for pasted PAGE TEXT on 2026-08-06
(`looksLikeHotelDetailPage`, `lodgingIntel.js:1855`) — it fires on the tab strip
(Overview/Prices/Reviews/…), which a URL does not contain. The bare-URL path was never
covered.

VERIFIED BY READING, NOT RUN — `lodgingIntel.js` imports `./dates` extensionless and
will not import under native ESM. Confirm by execution before fixing.

Fix is one clause: treat `/travel/hotels/entity/` in a URL as a detail page. Then decide
what it should SAY — probably "that's one hotel's page, paste the results list or add it
as a single option", not extraction. Test with the host's real URL.

Related, already recorded in §1 of WHERE_WE_ARE and still true: Google's `?q=` DISCARDS
dates and party (falls back to tomorrow / 1 night / 2 guests). Only `ts=` carries a trip.
Do NOT pass a hotel name as the place — driven, returns an empty page.

### 2. The grounding predicate — ONE governed commit, prerequisite already landed
`isGroundedItemQty` (`quantityProvenance.js:104`) is the sole predicate behind
`fieldState`, so it drives the whole admin backlog. It inverts:

```
crabFeast.p_crabs       cited      tier: primary     4 sources -> needs-research
crabFeast.p_softdrinks  researched tier: researched  1 source  -> correctable
```

Four ordered moves, in ONE commit:
1. point the cited purchases' `sources` at `costco-pork-2026` / `costco-chicken-2026` /
   `costco-groundbeef-2026` / `dmv-crab-2026` (all registered 2026-08-07);
2. `isGroundedItemQty` uses `isGroundedTier` + `>=2` corroboration, NOT `tier === 'researched'`;
3. map `primary` in `TIER_ALIASES` — it is unmapped, which is why first-hand dated
   evidence scores ungrounded;
4. re-baseline `4 — NO TRUST EXPANSION` in the SAME commit, outcomes stated.

ATTEMPTED 2026-08-07 AND REVERTED: broke 19 suites / 59 tests. **Do NOT loosen source
resolution to a length heuristic** — tried it, it lets any 12-character string ground a
price and breaks `a partially-resolving source list never grounds`.

### 3. The research pass (task #10, the binding constraint)
541/541 priced items now labelled, 0 unlabelled — but that made the number TRUE, not
better. `1.5%` cited across all 541; `8.9%` settled. 12+ playbooks at literal 0%:
anniversary, babyShower, bachelorParty, bacheloretteParty, birthday, boardMeeting,
bridalShower, cardParty, conference, crawfishBoil, dayParty, elopement.
Highest-traffic first: birthday, anniversary, babyShower, bridalShower.

Bar is `crabFeast.p_crabs` — named vendors, phone numbers, dated quotes, `claim` +
`sufficientWhen`. Two ratchets hold the line: 39 uncorroborated / 43 undated
(`researchPolicyCompliance.test.js`).

**43 undated claims CANNOT be back-filled honestly** — nobody knows when they were
researched. They get re-researched with a real date, or they stay unfalsifiable.

### 4. Still open from the board sitting (2026-08-07)
Scored 7/6/7 · 6.0/6.5 · 5/6/4. Nine findings taken; these were not:
radiogroup has no roving tabindex at the LIST level (the picker's own is done);
empty KIDS/MEAL/DIETARY columns need an em-dash or the track dropped;
Saarinen's keyboard pass (j/k, 1/2/3/0, commit-and-advance) — **do not build `/`-to-search,
the tier read found it is universally CMD-K**; the replies-only pass; the settle row
should carry primary weight and does not.

### 5. Mobbin tier read — to 9, then 10+
`docs/audits/2026-08-07_TIER_READ_DENSITY_TYPE_COMMIT_MOTION.md`. To 9: shell cap moves
from shell to a `--measure:720px` on prose (NEEDS A RULING — touches 4 rules on every
surface); panel 340 -> 440; bulk select. To 10+: third pane holds the ROLLUP not the
record; filter by who-owes-the-next-act; classify writes fact-stated vs structure-changed
and gate it in a test.

---

## HYGIENE
- `hostv2/test-results/.last-run.json` is TRACKED and now on `main`. Gitignore hole.
- `hostv2/review-shots/*.png` churn on every matrix run — `git checkout --` them.

## STALE-CLAIM WARNING — I got this wrong twice today
Two of "three known live issues" I repeated were not live:
- **air/ground have no Sections door — FIXED 2026-07-30** (`sectionDirectory.js:82`).
  I quoted a 2026-07-29 finding as current, nine days after it closed.
- **room-block "disconnected"** — the machinery is plainly there (`HostShellV2.jsx:3421`,
  `:10882`, `:11991`). Whether it is REACHABLE needs a drive. "I don't know" was the
  honest statement and I collapsed it into "broken".
Only the **per-day programme schema** is confirmed missing (`itinerary.js:201`).

**Check any open item against the code before working it.** Several on the list are
probably already done.

## TRAPS EARNED TODAY (full set in WHERE_WE_ARE §6)
- `[data-rail="1"]` is NOT desktop — shipped that misreading TWICE in one day.
- A structural cap must be matched by STRUCTURE: `:has(> X)` is a direct-child test.
- Walk the ancestor chain in the live DOM; do not grep for a cap.
- **Agreement between two detectors is not correctness** — built a second instrument to
  cross-check the first and it inherited the same blind spot. Both were wrong together,
  and only jest caught the damage.
- jest `expect(value, message)` is PLAYWRIGHT's API. Hit it twice.
- jest runs from `demo/`, NEVER `demo/hostv2/` — a compound `cd` gives a silent exit 1.
- Never pipe a background matrix through `tail` — you lose the failure detail.
