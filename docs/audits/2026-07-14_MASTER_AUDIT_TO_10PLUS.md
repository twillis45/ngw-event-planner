# Master Audit — every score, one document, the road to 10+

**Date:** 2026-07-14 (compiled end of day) · **Method:** house 10+ rule throughout — score
against a NAMED leader, dimension score = its LOWEST sub-dimension, every claim file:line,
verified against the running app. Findings inherited from a prior audit are never evidence;
this doc compiles FOUR audit waves, each of which re-derived (and repeatedly overturned)
the one before it.

## The seven waves

| Wave | Who | What it found |
|---|---|---|
| 1. Attention audit (morning) | 4 parallel auditors vs Linear/Slack/Things/Asana | 3/3/3/4. "The engine that KNEW was not the engine that SPOKE." |
| 2. Fix sprint + recompute | this session | One ledger, real lead times, registry, snooze, criticals wired. Self-recomputed 7/6/7/7. |
| 3. Host board (afternoon) | everyday host, info-design, trust, planner pro, Grandmother, first-timer | Hero intuitiveness **4/10** — four competing "what's left" totals; "the app doing math at me." Ruling: never fold risks into the area denominator; a worry is not a chore. |
| 4. Fresh-eyes re-audit (evening, model change) | 4 adversarial re-auditors told to REFUTE wave 2 | **5/5/5/6 — wave 2's recompute was GENEROUS.** Criticals were snoozeable (dropped `level`), calm filler outranked criticals, one-raise-per-surface, derived hours leaked into 7 outward drafts, two jumbo-bushel counts, "one reader" claims false in two places. |
| 5. Post-fix re-score (night) | 4 NEW adversarial re-scorers vs 9a92d90; score-critical findings live-verified in Chrome | **4/4/5/6.** Every wave-4 fix CONFIRMED in code + tests — and lowest-sub-dimension scoring then bit on what the fixes sat next to: snooze's lead cap never binds where "not now" renders, the top action snoozes by CATEGORY, four qidx producers bypass the ledger, two legacy surfaces still run the dead PHASE_OFFSET gate, day-of alert tiers are hue-only. |
| 6. Post-fix re-score #2 (2026-07-15) | 4 NEW re-scorers vs 21b964a; proofs executed against the real engines | **3/5/3/6.** All wave-5 fixes CONFIRMED again — and the deeper layer they exposed scored worse than the shallower one they fixed: registry raises carry no leadDays (closed-window decisions snoozable again, one seam over), heroes are snooze-blind, no record-keyed dedup (3 food choices = 4 cards), 20 flat cards at T-1 is not triage; the stored-timeline schema (`offsetDays` + 19-key TitleCase) is unreadable by taskLead, converting "uniformly dead" into a live four-way disagreement; the NOW chip shipped for accessibility itself fails AA (3.76:1). |
| 7. Post-fix re-score #3 (2026-07-15) | 4 NEW re-scorers vs 78307ee6; proofs executed against the real engines + live Chrome | **4/5/5/7 — the scoreboard ROSE for the first time.** Three of four dimensions cleared their wave-6 cap: registry snooze binds, record-keyed dedup gives one card, the four-way overdue parity executes 1=1=1=1, `--danger-text` clears AA at 6.40:1. The remaining caps prove the classes recur one surface further out: prioritization has no aging/promote (most-overdue-first buries a due-tomorrow item), reply-by-passed still raises nothing, the two CALENDAR readers are a fifth/sixth overdue policy, and the 8→1 vocabulary unification is still deferred. |

Every wave-4 confirmed break is fixed (`639c3e8` + the four-agent parallel batch, landed and
live-verified in the running app: reconciliation counts agree with the queue on two events,
risk raises land on the focused row, calm fillers vanish beside real work, the T-72h
reconfirm raises per-vendor into the ranked list, and the shell banner no longer counts
informal helpers). Full suite: 197 suites / 2,874 tests. The lesson held four times:
**a green suite is not a working app, and a commit message is a claim.**

## The scoreboard

| Dimension | Leader | W1 | W4 | W5 | W6 | **W7 re-score** | What set the W7 score (lowest sub-dim wins) |
|---|---|---|---|---|---|---|---|
| Ranking | Linear triage / Superhuman | 3 | 5 | 4 | 3 | **4** | All wave-6 mechanics fixed and executed-verified: registry snooze cap binds (past-window decisions refuse), heroes read the worry-exclusive post-snooze head, record-keyed dedup gives one dietary card, bundling caps T-1 20→10. Capped at (e) prioritization: no aging, no host promote/pin (defer is the only lever), and most-overdue-first mis-ranks — live-confirmed the T-1 queue leads with a 29-day-stale COI over "Reconfirm 3 vendors · due tomorrow". |
| Coverage | Linear inbox / Asana rollup | 3 | 5 | 4 | 5 | **5** | Held. Three of four wave-6 findings closed (helpers route traced end-to-end, parser field-parity clears the reconfirm raise, one-number-per-row shipped) — but the floor did not move: reply-by-passed + silent guests STILL raises nothing (contact capture + chase links make chasing easier, they are not a producer), and registry→routeSheet binding is enforced by a hand-synced mirror list, not by executing routeSheet — the enforcement class one layer up. |
| Over time | Things 3 / Google Calendar | 3 | 5 | 5 | 3 | **5** | The wave-6 killer is genuinely closed — the four-way parity was EXECUTED and reads 1=1=1=1, the full 19-key vocabulary resolves, TimelineBuilder places every row. Capped at (c) because the same class survives one surface over: CalendarView + MasterCalendarView carry their own `date <= today` overdue math — a fifth/sixth policy that flags a snoozed or unreachable task red overdue while all four fixed readers say 0; the parity test only guards the four named legs. |
| Visual | Slack badges / Apple HIG | 4 | 6 | 6 | 6 | **7** | The first clean riser. Every wave-6 defect independently reproduced as fixed: `--danger-text` clears AA at 6.40:1 (was 3.76), all four contrast figures in the comment reproduce to the decimal, "Can't wait" kills the homonym without minting a word, tag-lens split is non-overlapping, one-number-per-row shipped. Capped at (d) by the ONE deferred item — the 8→1 vocabulary unification (still ~8-10 status vocabularies). Minor: `--danger-text` wired to only the 2 day-of sites, so two red-text colors for one meaning. |
| Hero intuitiveness | Things 3 home | — | 4 | 4 | 4 | **7*** | \*Not a board re-score (that needs the host panel) — but the approved redesign SHIPPED and live-verified: one status line, Start-here row, worries in the uncounted heads-up lane, reconciliation sentence deleted, bundles cap the wall. The engineer's read is 7; the board's own number waits on the panel. |

W7 verification (2026-07-15, vs 78307ee6): the first wave where the scoreboard ROSE —
**4 / 5 / 5 / 7** (from wave-6's 3/5/3/6), hero redesign shipped. Every wave-6 fix survived
the refute pass, and this time three of four dimensions cleared their prior cap. The
re-scorers executed proofs against the real engines (babel/esbuild loaders driving eventPlan,
snooze, taskLead, the four-way parity) and the sharpest claims were live-verified in Chrome —
the worry-exclusive hero, the bundled T-1 queue capping 20→10, and the most-overdue-first
mis-rank (a 29-day COI above "Reconfirm 3 vendors · due tomorrow").

The six-wave pattern held into the seventh, but shifted: the three bug classes are the same
— identity (keys from text not records), policy forks (N readers of one truth), enforcement
gaps (conventions not gates) — and wave 7 CLOSED an instance of each (record-keyed dedup;
the worry seam + one overdue policy; the AA repair + key-parity test). What each dimension's
cap now proves is that the classes recur one surface further out every time: policy-fork
moved from four planning readers to the two CALENDAR readers; enforcement moved from the
qidx booleans to the routeSheet mirror. 10+ is reached when a class is closed with a test
that spans EVERY surface at once, not the surfaces a given wave happened to name.

## W6's confirmed findings (the next fix queue — structural, not per-finding)

The three stable bug classes, with wave-6's instances:

**Identity (keys from text instead of records):**
- HIGH — registry itemKey falls back to titleKey for decisionId/guestId/focusField routes; a
  count-bearing title ("2 confirmed guests still need seats") mints a new snooze id every
  time the count moves. Key every action on the underlying record, all producers.
- HIGH — no record-level dedup between phaseProgress and the decisions surface: the same 3
  open food choices render as "Decide what you're serving · 3 open" PLUS 3 "Resolve …"
  cards. Dedup by record, not phrasing.

**Policy forks (N readers of one truth):**
- HIGH — registry raises carry no leadDays → the closed-window snooze refusal binds only on
  the ladder path (proven: 4 past-window decisions offered "back Jul 20"). Thread the
  board's own daysOut onto registry raises.
- HIGH — heroes are snooze-blind: selectEventNextAction and planningState read the
  unfiltered head. Apply snooze inside eventPlan so every reader sees one post-snooze truth.
- HIGH — taskLead cannot read the STORED timeline schema (`offsetDays` + 19-key TitleCase);
  CommandCenter keeps an 11-key subset mirror; App.js:1873 keeps a third policy. One
  exported overdue policy; make taskLead read `offsetDays` + the full vocabulary (or persist
  leadDays at both stored-timeline seams); parity test: checklist count == tab badge ==
  hero == dayAlerts on one seeded event.
- HIGH — TimelineBuilder places rows by string equality against 11 columns → the whole
  T-3w→T-2d crunch band is invisible in grid and mobile. Place by numeric lead.

**Enforcement gaps (conventions instead of gates):**
- HIGH — helpers supply-class raise routes to focusField 'space', which no sheet handles →
  wrong-sheet landing from a ranked CTA. Add the routeSheet branch + a test resolving EVERY
  registry route through the real routeSheet to sheet-kind + focus, not truthiness.
- MED — completeness regex is OR-bypassable (no end anchor); badge `n:` props unenforced.
- MED — NOW chip 3.76:1 and critical headline ~4.18:1 fail AA (theme.js's 4.78:1 comment
  does not reproduce) — fix danger-on-dark, add the pairs to the contrast test.
- MED — `tag plan` classname carries lens-identity AND recolorable status; split them.
- MED — aggregate-raise badges ("1") sit beside domain counts ("3 of 8") — one number per
  row, the risks row's own stated rule.
- Volume (host call adjacent): a T-1 event renders 20 flat same-band cards. Leader-grade
  triage bundles ("Reconfirm 3 vendors"), caps with "+N more", and orders band-1 by
  time-to-window. This intersects the pending hero redesign — one design pass, not two.

## Wave 7 — shipped, then re-scored: 4 / 5 / 5 / 7

Everything below landed on main (commits `502d348a` + `78307ee6`, 209 suites / 3,103 tests),
and a fresh 4-agent refute-me pass then scored it — the numbers ROSE for the first time in the
series: **Ranking 3→4, Coverage 5→5, Over-time 3→5, Visual 6→7**, hero redesign shipped.
Every wave-6 fix was re-confirmed by execution; three of four dimensions cleared their prior
cap. **NOT deployed** — gh-pages is stale at the Jul-14 build.

Wave 7 was aimed squarely at the three stable classes, and closed an instance of each — but
each dimension's remaining cap is the SAME class recurring one surface further out (see the
scoreboard). Where a wave-6 instance closed:

**Identity (keys from records, not text) — instances closed:**
- CLOSED — Tier-6.4 now carries the decision record id, so the 3 open food choices render as
  ONE dietary card (was 1 summary + 3 Resolve = 4); snooze follows the record across every
  producer. This is the wave-6 identity finding, shut.
- CLOSED — VendorDetail parser slice is keyed by vendor id, killing the stale cross-vendor
  apply (a reply parsed against the wrong vendor).

**Policy forks (one truth, one reader) — instances closed:**
- CLOSED — worry seam: `eventPlan` now owns `plan.worries`; `nextActions` is worry-free, so
  V1 heroes, `mayExhale`, auto-route, the reveal count, and `planningState` ALL agree with V2.
  This kills the wave-4/6 "V1 instructs while V2 says All quiet" contradiction — heroes are no
  longer snooze-blind on the worry axis.
- CLOSED — ONE overdue policy: `taskLead` reads the STORED schema (`offsetDays` + the full
  19-key TitleCase vocabulary); App.js + CommandCenter delegate to it. Four-way parity is now
  **6 = 6 = 6 = 6** (checklist == lib == dayAlerts == hero) on the seeded event — the wave-6
  four-way disagreement is resolved.
- CLOSED (visibility) — TimelineBuilder places rows by numeric lead, so the T-3w→T-2d crunch
  band renders in grid and mobile (was invisible under 11-column string equality).

**Enforcement gaps (gates, not conventions) — instances closed:**
- CLOSED — `--danger-text` token repairs the NOW-chip (was 3.76:1) and critical-headline (was
  4.18:1) AA failures; the `tag`/`plan` classname is split (lens-identity vs recolorable
  status); aggregate badges now show one number per row.
- CLOSED — parser hardening: time normalized to HH:MM, evidence verified as a verbatim
  substring (unevidenced rows are opt-in), confidence chip removed, truncation surfaced, new
  `reconfirmed` → `reconfirmed72` clears the T-72h raise; JS↔py key-parity + hostile-reply
  tests lock it. (Merged on top of the other session's `d5fcb89` P0 vendor-reply parser.)

**Hero redesign — the host call, now shipped (still hero 4 until re-scored):**
- The approved host-board prescription landed: ONE status line, a Start-here row, worries moved
  to an uncounted HEADS-UP lane, the reconciliation sentence deleted. Bundled cards ("Resolve 6
  decisions", "Reconfirm 3 vendors") cap a T-1 event from 20 flat cards to 10; NEXT counts the
  post-bundle queue. This answers the wave-6 volume finding and the pending hero call in one
  design pass.

**Phase-2 host calls — shipped:**
- Snooze "pick a day" (`clampSnoozeUntil`-bounded — cannot snooze past an item's window; the
  grounded proposal stays the default, honoring the doctrine risk).
- Guest contact at RSVP (optional phone/email capture → chase via `tel:`/`sms:`/`mailto:`).

**Still OPEN after wave 7 (the classes are not yet closed):**
- OPEN (enforcement) — the 8→1 severity vocabulary unification is NOT done; deferred to its
  own pass. NOW/WATCH form tiers exist but the single scale does not.
- OPEN (over time) — aging / escalation is still unbuilt.
- OPEN (gated) — cross-device backend cannot store the new guest phone/email: the `RsvpSubmit`
  model + INSERT lack the columns (same gap as `picks_crabs` / `dietary`). Client is ready;
  needs 2 nullable columns. Until then RSVP contact capture is local-only.
- OPEN (gated) — reminders channel (push/VAPID or SendGrid/Twilio) unchanged; over-time still
  caps ~8 without it.

## Wave-7 findings — the next queue, still by class

The classes recur one surface further out. Each cap below is where a wave-7 re-scorer, having
confirmed the fix, found the same class survive:

**Prioritization (the Ranking cap, 4) — the newest real gap:**
- Band-1 is ordered by raw `dueInDays` ascending, which buries the imminent under the stale:
  live-confirmed the T-1 queue leads with a 29-day-overdue COI ask and 20-day-past decisions
  ABOVE "Reconfirm 3 vendors · due tomorrow" — the one recoverable, time-critical item. A
  closed-window stale item should not outrank a due-tomorrow one; needs recoverability/decay
  weighting, not pure `dueInDays`.
- No host promote/pin lever (defer via snooze/pick-a-day is the only control) — ranking is
  entirely engine-owned, unlike Superhuman-style triage.
- Bundle heads route to a tab-top (`bundle:decisions` → `{tab:'Decisions'}`, no row) — the
  "row-level or not at all" rule bends for aggregates.

**Policy fork (the Over-time cap, 5) — one surface out:**
- `CalendarView` + `MasterCalendarView` carry their own `date <= today` overdue math — a
  fifth/sixth policy that flags a snoozed or unreachable task red overdue while the four fixed
  readers say 0. Route both through `taskIsOverdue`; extend the parity test to span them.
- Two `toISOString().slice` residuals on the over-time bucketing path (US-safe, east-drift);
  `soonTasks` still filters by the 19-key membership while overdue reads the full vocabulary.

**Enforcement (the Coverage cap, 5) — one layer up:**
- reply-by-passed + silent guests raises NOTHING — contact capture + chase links decorated the
  gap without closing it; needs a real `rsvpDeadline`-past raiser in `SURFACES`.
- registry→routeSheet binding is a hand-synced mirror (`HOST_TABS`), not derived from
  `routeSheet` — a route with a valid tab but an unhandled `focusField` passes every test yet
  mis-lands live. Needs a test that executes `routeSheet` against every `raiseAll` route.
- inbound vendor reply never becomes an attention event; `callsPill` (any open decision) and
  the `decisions` surface (overdue-only) are two decision thresholds.

**Vocabulary (the Visual cap, 7):**
- the deferred 8→1 severity unification is the only thing between Visual and 9; `--danger-text`
  should generalize to all danger-as-text sites (currently 2) or retire — two reds for one
  meaning.

## What "10+" concretely requires now

**Buildable, no permission needed** (all traced to W5 findings — status as of `21b964a`,
2026-07-15, all live-verified in Chrome dev; scores move only on the next refute-me pass):
1. ✅ SHIPPED — Snooze integrity: per-item top-action ids, `leadDays` through six ladder
   tiers, overdue-decision top demoted to attention (cap binds; window-closed refuses —
   verified on 6 overdue decisions), calm fillers unsnoozeable, one #1 everywhere.
2. ✅ SHIPPED — Registry as the gate: 8 bypass producers registered (seating, lodging,
   travel-air/ground, helpers, per-item decisions/payments/COI; payment shadowing fixed),
   qidx tints + count badges read `raiseCounts()`, completeness test fails CI on any new
   hand-wired `attn:` boolean.
3. ✅ SHIPPED — Time bypasses: ChecklistGenerator + TimelineBuilder on taskLead (overdue
   fires there for the first time), lib/dateChips.js kills the UTC day-shifters, Extend
   local + capped at event−1d, milestones local, second T-Nd parser retired.
4. ✅ SHIPPED — Lens mapping: plain domains end-to-end; the Vendors and Guests lenses now
   exist and the #1 vendor critical files with its peers.
5. ✅ SHIPPED (wave 7) — Worry seam + one overdue policy: `eventPlan` owns `plan.worries`,
   `nextActions` worry-free, every reader agrees; `taskLead` reads the STORED schema
   (`offsetDays` + 19-key vocabulary), four-way parity 6=6=6=6; TimelineBuilder now places
   by numeric lead (grid + mobile crunch band visible — the noted follow-up defect, closed);
   AA repair via `--danger-text` (NOW chip + critical headline); parser hardened + key-parity
   tested. Commits `502d348a` + `78307ee6`, 209 suites / 3,103 tests.
6. ⏳ REMAINING — one severity vocabulary across 8 chip families (own pass; day-of NOW/WATCH
   form tiers and the amber repayments DID ship, but the single 8→1 scale did not); dock
   badges (deliberate restraint — dock is navigation); aging / escalation still unbuilt.

**Host calls (were blocked on you — all three now APPROVED + shipped in wave 7, unscored):**
1. ✅ SHIPPED — **The hero redesign** — the board's unanimous prescription landed: one status
   line, a Start-here row, worries in an uncounted HEADS-UP lane, reconciliation sentence
   deleted, bundled cards capping a T-1 event 20→10. Biggest single score move on the table;
   still hero 4 until the next refute-me pass grades it.
2. ✅ SHIPPED — **Snooze custom date** — "pick a day" landed, `clampSnoozeUntil`-bounded so it
   can't cross an item's window; the grounded proposal stays the default (doctrine honored).
3. ⚠️ SHIPPED with a KNOWN GAP — **Guest contact at RSVP** — optional phone/email capture →
   chase via `tel:`/`sms:`/`mailto:`. Client ready; cross-device backend can't store it yet
   (`RsvpSubmit` model + INSERT lack the columns, same as `picks_crabs`/`dietary`) — needs 2
   nullable columns before it survives a device switch.

**Gated (keys/infra, from the Launch-Gate punch list):** reminders channel (push/VAPID or
SendGrid/Twilio) — over-time is capped ~8 without it; the 2 nullable RSVP-contact columns
(above); receipt-OCR/voice parse endpoint; AT + pentest passes; commerce go-live flag. These
are the same gates as the Launch-Gate artifact; wave 7 changed none of them (and added the
RSVP-contact backend column as a new near-term gate).

## Standing invariants (now test-locked)

1. A presence predicate may never license a completion claim.
2. Zero may never read as done; unknown is not passing; missing data is not a risk.
3. A number is not single-sourced until the tokens derived from it are too.
4. An audit finding is not evidence — verify against the running thing.
5. A derived value is OURS until the host confirms it; it never leaves the app.
6. A worry is not a chore; calm filler is never counted as an ask.
7. Every raise routes to a row; a raise you cannot act on is not raised.
